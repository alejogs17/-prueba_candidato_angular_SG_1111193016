// 📌 ENTORNO DE DESARROLLO
// Este archivo se usa cuando ejecutas `ng serve` o `ng build --configuration development`
// La URL apunta al servidor Laravel local
export const environment = {
    production: false,
    apiUrl: 'http://127.0.0.1:8000/api/'
};
